version: '3.8'

services:
  localstack:
    image: localstack/localstack:4.14
    container_name: localstack_aws
    ports:
      - "4566:4566"
    environment:
      - SERVICES=s3
    volumes:
      - "./localstack_data:/var/lib/localstack"